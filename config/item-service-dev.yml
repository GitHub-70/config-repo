spring:
  application:
    name: item-service
    
#server:
  #port: 8001
  
eureka:
  client:
    service-url:
                 #云服务地点(没有)  就使用默认的地点       同时向两个注册中心注册
      defaultZone: http://eureka1:2001/eureka, http://eureka2:2002/eureka  
      
---
spring:
  profiles: item1
  
server:
  port: 8001
---
spring:
  profiles: item2

server:
  port: 8002

       